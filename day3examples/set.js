//데이터 타입에 상관없이 값을 집합으로 다뤄야 할 때
//Set 객체 생성
let mySet = new Set();

//1,2,3 차례대로 추가해보자.
mySet.add(1);
mySet.add(2);
mySet.add(3);

// 다시 1을 추가해보자.
//이미 1이란 값이 저장되어 있기 때문에 추가되지 않음.
mySet.add(1);
//크기는 어떻게?
// 3

//해당 값이 있는지 확인
//true

//해당 값을 지워보자.


//Set 객체의 모든 데이터를 읽어보자.


//몽땅 지워보자.
//저장되어 있는 모든 데이터가 삭제 됨.