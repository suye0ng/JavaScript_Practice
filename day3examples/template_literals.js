//일반적인 문자열 병합
function hello(name) {
    console.log("Hello " + name + ". Welcome!");
}

//문자열에 변수의 값을 병합할 때 효율적인 방법
//문자열은 백틱으로 감싸고 병합할 값은 ${변수} 형식으로 사용
function hello2(name) {
    let name2 = "John";

}

hello2("Jeremy");